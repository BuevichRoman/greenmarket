# Catalog Template v1.0 (PR-008) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship `catalog_template_v1.xlsx` — the official, normative Google Sheets/Excel Catalog Template v1.0 artifact — checked into the repo, matching the structural contract already fixed in `backend/app/validation/structure_validator.py`, with usability formatting (hints, examples, color coding), protection of service sheets, an Инструкция sheet, and example fixtures proven to pass the real Parser→Validator→Mapper pipeline.

**Architecture:** The `.xlsx` file is the single source of truth (colleague decision, 2026-07-13) — a Google Sheets copy is a derived, re-creatable artifact, not built or touched by this PR. The template's structure is generated by a small `app.catalog_template` package that imports its column contract directly from `structure_validator.py` (no duplicated constants, no drift possible). The generated `.xlsx` is committed to the repo like any other build output (analogous to a compiled asset) and is round-tripped through the existing `ExcelParser` in tests to prove it is 100% compatible with the already-merged pipeline — no parser/validator/mapper changes needed.

**Tech Stack:** Python, `openpyxl` (already a backend dependency), pytest, existing `ExcelParser`/`Validator`/`Mapper`.

---

## Context for the implementer

- Read `docs/02-domain/Catalog_Template.md` first — it's the existing normative doc this PR fleshes out into a real file. The sheet names, column names/order, and `_System` fields are **not up for redesign**, they're already fixed in code:
  - `backend/app/validation/structure_validator.py` — `CATALOG_SHEET`, `PRODUCT_GROUPS_SHEET`, `PRODUCTS_SHEET`, `INSTRUCTION_SHEET`, `SYSTEM_SHEET`, `CATALOG_COLUMNS`, `PRODUCT_GROUPS_COLUMNS`, `PRODUCTS_COLUMNS`, `SYSTEM_FIELDS`, `SUPPORTED_TEMPLATE_VERSIONS`.
  - `_System` sheet only ever contains `TemplateVersion`/`TemplateId` (CR-001) — no `PublicationKey`/`CatalogHash` in the file, ever.
- `database/seeders/001_product_groups.sql` / `002_products.sql` are the real dev/CI seed data (ProductGroup tree + Product list). This plan mirrors that exact data into the template's reference sheets and dropdown lists, so examples in the template line up with what `backend/tests` actually seeds in CI.
- `backend/app/parsing/excel_parser.py` reads any `.xlsx` into a `RawWorkbook` — this is what proves the generated template is structurally compatible; no Google API access is needed for this PR.
- Negative-path testing (broken structure, missing required fields) is **already fully covered** by `backend/tests/test_structure_validator.py` and `test_semantic_validator.py` against hand-built workbooks using this exact same contract — do not duplicate those tests here. This plan only adds tests for the parts not yet covered: that the *actual checked-in file* round-trips correctly and that realistic filled-in examples pass the *full* pipeline (`Validator` + `Mapper`) end-to-end.

## File Structure

- Create: `backend/app/catalog_template/__init__.py` — empty, new subpackage (matches `app/parsing/`, `app/validation/`, `app/mapping/` convention).
- Create: `backend/app/catalog_template/data.py` — static reference data: column hints/examples, `Товарные группы`/`Товарные позиции` reference rows (mirrors seeders), Инструкция text, `TemplateVersion`/`TemplateId` constants.
- Create: `backend/app/catalog_template/build.py` — `build_workbook()` builds the full 5-sheet `openpyxl.Workbook`; `main()` CLI writes it to `docs/02-domain/templates/catalog_template_v1.xlsx`.
- Create: `backend/app/catalog_template/build_examples.py` — reuses `build_workbook()` with filled catalog rows to produce the `partial`/`full` example fixtures.
- Create: `backend/tests/test_catalog_template_builder.py` — unit tests on `build_workbook()`'s in-memory structure/formatting/protection.
- Create: `backend/tests/test_catalog_template_artifact.py` — integration tests reading the *committed* `.xlsx` files through `ExcelParser` + `Validator` + `Mapper`.
- Create (generated binary, not hand-written): `docs/02-domain/templates/catalog_template_v1.xlsx`.
- Create (generated binary, not hand-written): `docs/02-domain/templates/examples/catalog_template_v1_partial.xlsx`, `docs/02-domain/templates/examples/catalog_template_v1_full.xlsx`.
- Modify: `docs/02-domain/Catalog_Template.md` — document the `.xlsx` artifact as the normative source, Google Sheets as derived.
- Modify: `docs/02-domain/Publication_Model.md` — one-sentence cross-reference to the artifact path.
- Modify: `README.md` — mention where the template artifact lives.

---

### Task 1: Reference data module

**Files:**
- Create: `backend/app/catalog_template/__init__.py`
- Create: `backend/app/catalog_template/data.py`
- Test: `backend/tests/test_catalog_template_builder.py` (started here, extended in Task 2)

- [ ] **Step 1: Create the empty package init**

```bash
mkdir -p backend/app/catalog_template
touch backend/app/catalog_template/__init__.py
```

- [ ] **Step 2: Write the failing test for column hints consistency**

```python
# backend/tests/test_catalog_template_builder.py
from app.catalog_template.data import COLUMN_HINTS, GROUP_ROWS, PRODUCT_ROWS, TEMPLATE_ID, TEMPLATE_VERSION
from app.validation.structure_validator import CATALOG_COLUMNS, SUPPORTED_TEMPLATE_VERSIONS


def test_column_hints_cover_exactly_the_catalog_columns():
    assert set(COLUMN_HINTS) == {column.name for column in CATALOG_COLUMNS}


def test_template_version_is_supported():
    assert TEMPLATE_VERSION in SUPPORTED_TEMPLATE_VERSIONS


def test_group_rows_and_product_rows_are_internally_consistent():
    group_ids = {row[0] for row in GROUP_ROWS}
    parent_ids = {row[1] for row in GROUP_ROWS if row[1] is not None}
    assert parent_ids <= group_ids  # каждый ParentProductGroupId существует среди ProductGroupId

    product_group_ids = {row[1] for row in PRODUCT_ROWS}
    assert product_group_ids <= group_ids  # каждый товар ссылается на существующую группу
```

- [ ] **Step 3: Run test to verify it fails**

Run: `cd backend && uv run pytest tests/test_catalog_template_builder.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'app.catalog_template.data'`

- [ ] **Step 4: Write `data.py`**

```python
# backend/app/catalog_template/data.py
"""Статические данные для Catalog Template v1.0 (PR-008): подсказки колонок,
справочные строки «Товарные группы»/«Товарные позиции» (зеркалят
database/seeders/001_product_groups.sql и 002_products.sql — тот же порядок,
те же названия, чтобы примеры в шаблоне совпадали с тем, что реально
засеяно в dev/CI), текст листа «Инструкция», метаданные `_System`.

Единственный источник структуры (имена/порядок/обязательность колонок) —
app.validation.structure_validator; здесь только данные для их заполнения.
"""

TEMPLATE_VERSION = "1.0"
TEMPLATE_ID = "catalog-template-v1"

COLUMN_HINTS = {
    "SellerProductId": (
        "Служебное поле, скрыто. Не заполняется продавцом — сервер присваивает "
        "идентификатор автоматически после первой успешной публикации строки."
    ),
    "Наименование продавца": "Название товара, которое увидит покупатель. Пример: «Апельсины оптом».",
    "Товарная группа GreenMarket": (
        "Товарная группа из выпадающего списка (см. лист «Товарные группы»). Пример: «Цитрусовые»."
    ),
    "Товарная позиция GreenMarket": (
        "Товарная позиция из выпадающего списка (см. лист «Товарные позиции»). "
        "Если подходящей позиции нет — выберите «Прочее», товар пройдёт модерацию. Пример: «Апельсин»."
    ),
    "Цена": "Цена за единицу продажи, число, не может быть отрицательным. Пример: 99.5.",
    "Единица продажи": "Единица, в которой продаётся товар: кг, г, шт, уп, л, мл. Пример: «кг».",
    "Остаток": "Доступное количество в указанной единице продажи, число, не может быть отрицательным. Пример: 10.",
    "Описание": "Краткое описание товара для покупателя. Необязательно.",
    "Дополнительные характеристики": "Любые дополнительные характеристики товара текстом. Необязательно.",
}

# (name, parent_name_or_None) — порядок в точности как в database/seeders/001_product_groups.sql
_GROUP_DEFS = [
    ("Фрукты", None),
    ("Овощи", None),
    ("Молочные продукты", None),
    ("Мясо", None),
    ("Рыба", None),
    ("Напитки", None),
    ("Цитрусовые", "Фрукты"),
    ("Яблоки", "Фрукты"),
    ("Бананы", "Фрукты"),
    ("Томаты", "Овощи"),
    ("Огурцы", "Овощи"),
    ("Картофель", "Овощи"),
    ("Молоко", "Молочные продукты"),
    ("Сыр", "Молочные продукты"),
    ("Йогурты", "Молочные продукты"),
]

# (name, group_name) — в точности как в database/seeders/002_products.sql
_PRODUCT_DEFS = [
    ("Апельсин", "Цитрусовые"),
    ("Лимон", "Цитрусовые"),
    ("Банан", "Бананы"),
    ("Яблоко", "Яблоки"),
    ("Помидор", "Томаты"),
    ("Огурец", "Огурцы"),
    ("Картофель", "Картофель"),
    ("Лук", "Овощи"),
    ("Молоко", "Молоко"),
    ("Сыр", "Сыр"),
    ("Йогурт", "Йогурты"),
    ("Говядина", "Мясо"),
    ("Курица", "Мясо"),
    ("Сардина", "Рыба"),
    ("Тунец", "Рыба"),
    ("Минеральная вода", "Напитки"),
]


def _build_group_rows() -> list[list[object]]:
    rows: list[list[object]] = []
    id_by_name: dict[str, int] = {}
    for next_id, (name, parent_name) in enumerate(_GROUP_DEFS, start=1):
        parent_id = id_by_name[parent_name] if parent_name else None
        id_by_name[name] = next_id
        rows.append([next_id, parent_id, name])
    return rows


def _build_product_rows(group_rows: list[list[object]]) -> list[list[object]]:
    group_id_by_name = {row[2]: row[0] for row in group_rows}
    return [
        [product_id, group_id_by_name[group_name], name]
        for product_id, (name, group_name) in enumerate(_PRODUCT_DEFS, start=1)
    ]


GROUP_ROWS = _build_group_rows()
PRODUCT_ROWS = _build_product_rows(GROUP_ROWS)

# Списки для выпадающих списков «Каталог»: строятся из тех же данных, что и
# справочные листы, чтобы не могли разойтись. "Прочее" — не товар, это
# зарезервированное значение (app.validation.semantic_validator._OTHER_PRODUCT_PLACEHOLDER).
GROUP_NAMES_FOR_DROPDOWN = [row[2] for row in GROUP_ROWS]
PRODUCT_NAMES_FOR_DROPDOWN = [row[2] for row in PRODUCT_ROWS] + ["Прочее"]

INSTRUCTION_LINES = [
    "Инструкция по заполнению каталога GreenMarket",
    "",
    "1. Как сделать копию шаблона",
    (
        "В Google Drive откройте официальный шаблон GreenMarket, затем "
        "«Файл» → «Создать копию». Работайте только в своей копии — исходный "
        "шаблон никогда не редактируется напрямую."
    ),
    "",
    "2. Как заполнить каталог",
    (
        "На листе «Каталог» одна строка = один товар. Заполните обязательные "
        "поля (выделены жёлтым): «Наименование продавца», «Товарная группа "
        "GreenMarket», «Цена», «Единица продажи», «Остаток». Товарную группу и "
        "товарную позицию выбирайте из выпадающего списка. Если подходящей "
        "товарной позиции нет — выберите «Прочее»: товар будет опубликован и "
        "пройдёт модерацию администратором. Новый товар — просто новая строка."
    ),
    "",
    "3. Как расшарить таблицу на Service Account",
    (
        "Нажмите «Настройки доступа» и добавьте e-mail Service Account "
        "GreenMarket (указан в личном кабинете продавца) с правом «Читатель». "
        "GreenMarket никогда не изменяет вашу таблицу — только читает её при публикации."
    ),
    "",
    "4. Как опубликовать каталог",
    (
        "Скопируйте ссылку на таблицу и вставьте её в личном кабинете продавца, "
        "нажмите «Опубликовать». Результат проверки (успех или список ошибок "
        "построчно) придёт в личном кабинете."
    ),
    "",
    "5. Типичные ошибки",
    "— Изменены названия или порядок колонок на листе «Каталог» — не создавайте и не переименовывайте колонки вручную.",
    "— Товарная группа введена текстом вручную, а не выбрана из выпадающего списка.",
    "— Отрицательная или нечисловая цена/остаток.",
    "— Правки на листах «Товарные группы», «Товарные позиции» или «_System» — эти листы только для чтения, изменения на них сервер не учитывает.",
]
```

- [ ] **Step 5: Run test to verify it passes**

Run: `cd backend && uv run pytest tests/test_catalog_template_builder.py -v`
Expected: PASS (3 tests)

- [ ] **Step 6: Commit**

```bash
git add backend/app/catalog_template/__init__.py backend/app/catalog_template/data.py backend/tests/test_catalog_template_builder.py
git commit -m "feat(catalog-template): add reference data module for template v1.0"
```

---

### Task 2: Builder core

**Files:**
- Create: `backend/app/catalog_template/build.py`
- Test: `backend/tests/test_catalog_template_builder.py` (extend)

- [ ] **Step 1: Add failing tests for the built workbook's structure/formatting/protection**

Append to `backend/tests/test_catalog_template_builder.py`:

```python
from app.catalog_template.build import build_workbook
from app.validation.structure_validator import (
    CATALOG_SHEET,
    INSTRUCTION_SHEET,
    PRODUCT_GROUPS_SHEET,
    PRODUCTS_SHEET,
    REQUIRED_SHEETS,
    SYSTEM_SHEET,
)


def _sheet_values(ws):
    return [list(row) for row in ws.iter_rows(values_only=True)]


def test_all_required_sheets_present():
    wb = build_workbook()
    assert set(wb.sheetnames) == set(REQUIRED_SHEETS)


def test_catalog_header_matches_structure_validator_contract():
    wb = build_workbook()
    header = _sheet_values(wb[CATALOG_SHEET])[0]
    assert header == [column.name for column in CATALOG_COLUMNS]


def test_catalog_service_column_is_hidden():
    wb = build_workbook()
    assert wb[CATALOG_SHEET].column_dimensions["A"].hidden is True


def test_catalog_sheet_is_not_sheet_protected():
    # Продавец должен свободно добавлять/удалять/менять строки — служебные
    # листы защищены, рабочий лист «Каталог» нет (Catalog_Template.md).
    wb = build_workbook()
    assert wb[CATALOG_SHEET].protection.sheet is False


def test_reference_and_service_sheets_are_protected():
    wb = build_workbook()
    for name in (PRODUCT_GROUPS_SHEET, PRODUCTS_SHEET, INSTRUCTION_SHEET, SYSTEM_SHEET):
        assert wb[name].protection.sheet is True, f"{name} должен быть защищён от редактирования"


def test_system_sheet_is_hidden_and_matches_data_module():
    wb = build_workbook()
    ws = wb[SYSTEM_SHEET]
    assert ws.sheet_state == "hidden"
    values = dict(_sheet_values(ws))
    assert values == {"TemplateVersion": TEMPLATE_VERSION, "TemplateId": TEMPLATE_ID}


def test_reference_sheets_match_seed_data():
    wb = build_workbook()
    assert _sheet_values(wb[PRODUCT_GROUPS_SHEET])[1:] == GROUP_ROWS
    assert _sheet_values(wb[PRODUCTS_SHEET])[1:] == PRODUCT_ROWS


def test_every_catalog_header_cell_has_a_comment():
    wb = build_workbook()
    ws = wb[CATALOG_SHEET]
    for col_index in range(1, len(CATALOG_COLUMNS) + 1):
        assert ws.cell(row=1, column=col_index).comment is not None


def test_price_and_stock_columns_have_non_negative_validation():
    wb = build_workbook()
    ws = wb[CATALOG_SHEET]
    refs = [str(dv.sqref) for dv in ws.data_validations.dataValidation if dv.type == "decimal"]
    assert any(ref.startswith("E2") for ref in refs)
    assert any(ref.startswith("G2") for ref in refs)


def test_group_and_product_dropdowns_are_within_excel_inline_limit():
    wb = build_workbook()
    ws = wb[CATALOG_SHEET]
    list_validations = [dv for dv in ws.data_validations.dataValidation if dv.type == "list"]
    assert list_validations
    for dv in list_validations:
        assert len(dv.formula1) <= 255


def test_build_workbook_is_deterministic():
    first, second = build_workbook(), build_workbook()
    for name in first.sheetnames:
        assert _sheet_values(first[name]) == _sheet_values(second[name])


def test_build_workbook_accepts_prefilled_catalog_rows():
    row = [None, "Апельсины оптом", "Цитрусовые", "Апельсин", 99.5, "кг", 10, "", ""]
    wb = build_workbook(catalog_rows=[row])
    assert _sheet_values(wb[CATALOG_SHEET])[1] == row
```

- [ ] **Step 2: Run to verify failure**

Run: `cd backend && uv run pytest tests/test_catalog_template_builder.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'app.catalog_template.build'`

- [ ] **Step 3: Write `build.py`**

```python
# backend/app/catalog_template/build.py
"""Строит официальный Catalog Template v1.0 (PR-008).

Единственный источник структуры (листы, колонки, обязательность,
`_System`) — app.validation.structure_validator: этот модуль не хранит
собственной копии контракта, чтобы шаблон не мог разойтись с
Parser/Validator/Mapper.

Порождённый .xlsx — нормативный артефакт (docs/02-domain/Catalog_Template.md).
Google Sheets-копия для продавцов — производная: создаётся импортом этого
файла в Google Drive и может быть пересоздана из него в любой момент; сама
она источником истины не является (решение колонки от 2026-07-13).
"""

from __future__ import annotations

from pathlib import Path

from openpyxl import Workbook
from openpyxl.comments import Comment
from openpyxl.styles import PatternFill
from openpyxl.worksheet.datavalidation import DataValidation
from openpyxl.worksheet.worksheet import Worksheet

from app.catalog_template.data import (
    COLUMN_HINTS,
    GROUP_NAMES_FOR_DROPDOWN,
    GROUP_ROWS,
    INSTRUCTION_LINES,
    PRODUCT_NAMES_FOR_DROPDOWN,
    PRODUCT_ROWS,
    TEMPLATE_ID,
    TEMPLATE_VERSION,
)
from app.validation.structure_validator import (
    CATALOG_COLUMNS,
    CATALOG_SHEET,
    INSTRUCTION_SHEET,
    PRODUCT_GROUPS_COLUMNS,
    PRODUCTS_COLUMNS,
    PRODUCTS_SHEET,
    PRODUCT_GROUPS_SHEET,
    SYSTEM_SHEET,
)

OUTPUT_PATH = Path(__file__).resolve().parents[3] / "docs" / "02-domain" / "templates" / "catalog_template_v1.xlsx"

_MAX_DATA_ROW = 1000  # запас строк под форматирование/валидацию для будущих товаров продавца

_REQUIRED_FILL = PatternFill(start_color="FFF4CC", end_color="FFF4CC", fill_type="solid")
_OPTIONAL_FILL = PatternFill(start_color="E2EFDA", end_color="E2EFDA", fill_type="solid")
_SERVICE_FILL = PatternFill(start_color="D9D9D9", end_color="D9D9D9", fill_type="solid")


def build_workbook(catalog_rows: list[list[object]] | None = None) -> Workbook:
    workbook = Workbook()
    workbook.remove(workbook.active)

    _build_catalog_sheet(workbook, catalog_rows or [])
    _build_reference_sheet(workbook, PRODUCT_GROUPS_SHEET, PRODUCT_GROUPS_COLUMNS, GROUP_ROWS)
    _build_reference_sheet(workbook, PRODUCTS_SHEET, PRODUCTS_COLUMNS, PRODUCT_ROWS)
    _build_instruction_sheet(workbook)
    _build_system_sheet(workbook)

    return workbook


def _build_catalog_sheet(workbook: Workbook, catalog_rows: list[list[object]]) -> None:
    ws = workbook.create_sheet(CATALOG_SHEET, 0)

    for col_index, column in enumerate(CATALOG_COLUMNS, start=1):
        cell = ws.cell(row=1, column=col_index, value=column.name)
        cell.comment = Comment(COLUMN_HINTS[column.name], "GreenMarket")
        if column.name == "SellerProductId":
            cell.fill = _SERVICE_FILL
        elif column.required:
            cell.fill = _REQUIRED_FILL
        else:
            cell.fill = _OPTIONAL_FILL

    for row_index, row in enumerate(catalog_rows, start=2):
        for col_index, value in enumerate(row, start=1):
            ws.cell(row=row_index, column=col_index, value=value)

    _apply_catalog_validation(ws)
    ws.column_dimensions["A"].hidden = True


def _quoted_list(values: list[str]) -> str:
    return '"' + ",".join(values) + '"'


def _apply_catalog_validation(ws: Worksheet) -> None:
    last_row = max(ws.max_row, _MAX_DATA_ROW)

    group_validation = DataValidation(type="list", formula1=_quoted_list(GROUP_NAMES_FOR_DROPDOWN), allow_blank=True)
    group_validation.errorTitle = "Неизвестная товарная группа"
    group_validation.error = "Выберите товарную группу из списка (см. лист «Товарные группы»)"
    ws.add_data_validation(group_validation)
    group_validation.add(f"C2:C{last_row}")

    product_validation = DataValidation(type="list", formula1=_quoted_list(PRODUCT_NAMES_FOR_DROPDOWN), allow_blank=True)
    product_validation.errorTitle = "Неизвестная товарная позиция"
    product_validation.error = "Выберите товарную позицию из списка или «Прочее» (см. лист «Товарные позиции»)"
    ws.add_data_validation(product_validation)
    product_validation.add(f"D2:D{last_row}")

    price_validation = DataValidation(type="decimal", operator="greaterThanOrEqual", formula1="0", allow_blank=True)
    price_validation.errorTitle = "Некорректная цена"
    price_validation.error = "Цена не может быть отрицательной"
    ws.add_data_validation(price_validation)
    price_validation.add(f"E2:E{last_row}")

    stock_validation = DataValidation(type="decimal", operator="greaterThanOrEqual", formula1="0", allow_blank=True)
    stock_validation.errorTitle = "Некорректный остаток"
    stock_validation.error = "Остаток не может быть отрицательным"
    ws.add_data_validation(stock_validation)
    stock_validation.add(f"G2:G{last_row}")

    unit_validation = DataValidation(
        type="list", formula1='"кг,г,шт,уп,л,мл"', allow_blank=True, showErrorMessage=True
    )
    unit_validation.errorStyle = "warning"
    unit_validation.errorTitle = "Необычная единица продажи"
    unit_validation.error = "Обычно используется: кг, г, шт, уп, л, мл. При необходимости можно ввести своё значение."
    ws.add_data_validation(unit_validation)
    unit_validation.add(f"F2:F{last_row}")


def _build_reference_sheet(workbook: Workbook, sheet_name: str, columns, rows: list[list[object]]) -> None:
    ws = workbook.create_sheet(sheet_name)
    for col_index, column in enumerate(columns, start=1):
        cell = ws.cell(row=1, column=col_index, value=column.name)
        cell.fill = _SERVICE_FILL
    for row_index, row in enumerate(rows, start=2):
        for col_index, value in enumerate(row, start=1):
            ws.cell(row=row_index, column=col_index, value=value)
    ws.protection.sheet = True


def _build_instruction_sheet(workbook: Workbook) -> None:
    ws = workbook.create_sheet(INSTRUCTION_SHEET)
    for row_index, line in enumerate(INSTRUCTION_LINES, start=1):
        ws.cell(row=row_index, column=1, value=line)
    ws.column_dimensions["A"].width = 100
    ws.protection.sheet = True


def _build_system_sheet(workbook: Workbook) -> None:
    ws = workbook.create_sheet(SYSTEM_SHEET)
    ws.append(["TemplateVersion", TEMPLATE_VERSION])
    ws.append(["TemplateId", TEMPLATE_ID])
    ws.protection.sheet = True
    ws.sheet_state = "hidden"


def main() -> None:
    workbook = build_workbook()
    OUTPUT_PATH.parent.mkdir(parents=True, exist_ok=True)
    workbook.save(OUTPUT_PATH)
    print(f"Шаблон сохранён: {OUTPUT_PATH}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd backend && uv run pytest tests/test_catalog_template_builder.py -v`
Expected: PASS (all tests, ~14)

- [ ] **Step 5: Commit**

```bash
git add backend/app/catalog_template/build.py backend/tests/test_catalog_template_builder.py
git commit -m "feat(catalog-template): build the Catalog Template v1.0 workbook"
```

---

### Task 3: Generate and commit the master artifact

**Files:**
- Create (binary): `docs/02-domain/templates/catalog_template_v1.xlsx`
- Create: `backend/tests/test_catalog_template_artifact.py`

- [ ] **Step 1: Write the failing test for the committed master file**

```python
# backend/tests/test_catalog_template_artifact.py
from pathlib import Path

from app.parsing.excel_parser import ExcelParser
from app.validation.structure_validator import StructureValidator

REPO_ROOT = Path(__file__).resolve().parents[2]
MASTER_TEMPLATE_PATH = REPO_ROOT / "docs" / "02-domain" / "templates" / "catalog_template_v1.xlsx"


def test_master_template_file_exists():
    assert MASTER_TEMPLATE_PATH.is_file(), "Запусти `uv run python -m app.catalog_template.build` и закоммить файл"


def test_master_template_passes_structure_validation():
    # "Новый пустой шаблон" из акцептанс-критериев PR-008: 0 строк каталога,
    # структура должна быть валидна как есть, до заполнения продавцом.
    workbook = ExcelParser().parse(MASTER_TEMPLATE_PATH)

    result = StructureValidator().validate(workbook)

    assert result.is_valid, result.errors
```

- [ ] **Step 2: Run to verify it fails**

Run: `cd backend && uv run pytest tests/test_catalog_template_artifact.py -v`
Expected: FAIL — `test_master_template_file_exists` fails, file doesn't exist yet

- [ ] **Step 3: Generate the file**

```bash
cd backend && uv run python -m app.catalog_template.build
```

Expected output: `Шаблон сохранён: .../docs/02-domain/templates/catalog_template_v1.xlsx`

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd backend && uv run pytest tests/test_catalog_template_artifact.py -v`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit (binary file included)**

```bash
git add docs/02-domain/templates/catalog_template_v1.xlsx backend/tests/test_catalog_template_artifact.py
git commit -m "feat(catalog-template): generate and commit Catalog Template v1.0 artifact"
```

---

### Task 4: Example fixtures (partial / full) through the real pipeline

**Files:**
- Create: `backend/app/catalog_template/build_examples.py`
- Create (binary): `docs/02-domain/templates/examples/catalog_template_v1_partial.xlsx`
- Create (binary): `docs/02-domain/templates/examples/catalog_template_v1_full.xlsx`
- Test: `backend/tests/test_catalog_template_artifact.py` (extend)

- [ ] **Step 1: Write `build_examples.py`**

```python
# backend/app/catalog_template/build_examples.py
"""Генерирует заполненные примеры Catalog Template v1.0 (PR-008 acceptance
criteria: "частично заполненный", "полностью заполненный") — те же 5 листов,
что и мастер-шаблон (app.catalog_template.build.build_workbook), с
заполненным листом «Каталог». Используются в тестах для проверки полного
пайплайна Parser -> Validator -> Mapper на реалистичных данных.
"""

from pathlib import Path

from app.catalog_template.build import build_workbook

EXAMPLES_DIR = Path(__file__).resolve().parents[3] / "docs" / "02-domain" / "templates" / "examples"

# SellerProductId пуст — все строки новые, ещё не публиковались.
PARTIAL_ROWS = [
    [None, "Апельсины оптом", "Цитрусовые", "Апельсин", 99.5, "кг", 10, "", ""],
    [None, "Минеральная вода 'Летняя'", "Напитки", "", 45, "л", 50, "", ""],
]

FULL_ROWS = [
    [
        None,
        "Апельсины оптом",
        "Цитрусовые",
        "Апельсин",
        99.5,
        "кг",
        10,
        "Свежие апельсины из Абхазии",
        "Сорт: Washington navel",
    ],
    [
        None,
        "Молоко фермерское 3.2%",
        "Молоко",
        "Молоко",
        89,
        "л",
        30,
        "Цельное коровье молоко",
        "Жирность 3.2%",
    ],
    [
        None,
        "Мандарины абхазские",
        "Цитрусовые",
        "Прочее",
        180,
        "кг",
        25,
        "Новый товар, ожидает модерации",
        "Сладкие, тонкая кожура",
    ],
]


def main() -> None:
    EXAMPLES_DIR.mkdir(parents=True, exist_ok=True)

    build_workbook(catalog_rows=PARTIAL_ROWS).save(EXAMPLES_DIR / "catalog_template_v1_partial.xlsx")
    build_workbook(catalog_rows=FULL_ROWS).save(EXAMPLES_DIR / "catalog_template_v1_full.xlsx")

    print(f"Примеры сохранены: {EXAMPLES_DIR}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Generate the example files**

```bash
cd backend && uv run python -m app.catalog_template.build_examples
```

Expected output: `Примеры сохранены: .../docs/02-domain/templates/examples`

- [ ] **Step 3: Write the failing pipeline tests**

Append to `backend/tests/test_catalog_template_artifact.py`:

```python
from app.infrastructure.repositories.product_group_repository import ProductGroupRepository
from app.infrastructure.repositories.product_repository import ProductRepository
from app.mapping.mapper import Mapper
from app.validation.business_validator import BusinessValidator
from app.validation.semantic_validator import SemanticValidator
from app.validation.validator import Validator

EXAMPLES_DIR = REPO_ROOT / "docs" / "02-domain" / "templates" / "examples"
PARTIAL_EXAMPLE_PATH = EXAMPLES_DIR / "catalog_template_v1_partial.xlsx"
FULL_EXAMPLE_PATH = EXAMPLES_DIR / "catalog_template_v1_full.xlsx"


def _make_validator(session) -> Validator:
    return Validator(
        StructureValidator(),
        SemanticValidator(ProductGroupRepository(session), ProductRepository(session)),
        BusinessValidator(),
    )


def test_partial_example_passes_full_pipeline(session):
    workbook = ExcelParser().parse(PARTIAL_EXAMPLE_PATH)

    result = _make_validator(session).validate(workbook)
    assert result.is_valid, result.errors

    model = Mapper().map(workbook, result, seller_id=1)
    assert len(model.products) == 2


def test_full_example_passes_full_pipeline(session):
    workbook = ExcelParser().parse(FULL_EXAMPLE_PATH)

    result = _make_validator(session).validate(workbook)
    assert result.is_valid, result.errors

    model = Mapper().map(workbook, result, seller_id=1)
    assert len(model.products) == 3
    assert model.products[2].product_name == "Прочее"
```

- [ ] **Step 4: Run to verify pass**

Run: `cd backend && uv run pytest tests/test_catalog_template_artifact.py -v`
Expected: PASS (4 tests). Requires local MySQL with migrations+seeders applied, same as the rest of the suite (`tests/fixtures/platform_stub.sql` + `database/migrations/00{1..6}_*.sql` + `database/seeders/00{1,2}_*.sql`).

- [ ] **Step 5: Commit**

```bash
git add backend/app/catalog_template/build_examples.py backend/tests/test_catalog_template_artifact.py \
  docs/02-domain/templates/examples/catalog_template_v1_partial.xlsx \
  docs/02-domain/templates/examples/catalog_template_v1_full.xlsx
git commit -m "feat(catalog-template): add partial/full example fixtures for the full pipeline"
```

---

### Task 5: Documentation updates

**Files:**
- Modify: `docs/02-domain/Catalog_Template.md`
- Modify: `docs/02-domain/Publication_Model.md`
- Modify: `README.md`

- [ ] **Step 1: Add the artifact section to `Catalog_Template.md`**

Insert a new section right after "## Назначение" (before "## Архитектурные принципы"):

```markdown
## Нормативный артефакт

Начиная с PR-008 нормативным артефактом шаблона является файл
[`catalog_template_v1.xlsx`](templates/catalog_template_v1.xlsx), хранящийся
в этом репозитории. Файл версионируется в Git, открывается и в Excel, и в
Google Sheets без потери структуры, используется в автоматических тестах
Parser/Validator/Mapper (`backend/tests/test_catalog_template_artifact.py`) и
не зависит от конкретного Google-аккаунта или прав доступа.

Официальная Google Sheets Template, которую продавцы копируют себе через
«Создать копию», — производный артефакт: импортируется из этого файла в
Google Drive и может быть пересоздана из него в любой момент. Google-таблица
источником истины не является.

Примеры заполненного шаблона (для разработчиков и колле-ревью, не для
продавцов): [`templates/examples/catalog_template_v1_partial.xlsx`](templates/examples/catalog_template_v1_partial.xlsx),
[`templates/examples/catalog_template_v1_full.xlsx`](templates/examples/catalog_template_v1_full.xlsx).

Файл порождается детерминированно скриптом `backend/app/catalog_template/build.py`
(`uv run python -m app.catalog_template.build` из `backend/`) — при изменении
контракта колонок в `structure_validator.py` шаблон нужно перегенерировать и
закоммитить заново, а не редактировать вручную.
```

- [ ] **Step 2: Add one cross-reference sentence to `Publication_Model.md`**

In the "## Источник данных" section, after the existing sentence ending "...определён в [Catalog_Template.md](Catalog_Template.md)." add:

```markdown
Нормативный файл шаблона — `docs/02-domain/templates/catalog_template_v1.xlsx` (PR-008); Google Sheets, которую заполняет продавец, — его производная копия.
```

- [ ] **Step 3: Mention the artifact in `README.md`**

Read `README.md` first to find the section listing `docs/` contents (likely near a repo-structure table), and add one line pointing to `docs/02-domain/templates/catalog_template_v1.xlsx` as the official catalog template artifact. Match the existing table/list formatting already used in that file — don't introduce a new section style.

- [ ] **Step 4: Commit**

```bash
git add docs/02-domain/Catalog_Template.md docs/02-domain/Publication_Model.md README.md
git commit -m "docs(catalog-template): document the .xlsx artifact as the normative source"
```

---

### Task 6: Full verification + package for colleague review

**Files:** none new — verification and packaging only.

- [ ] **Step 1: Run the full backend test suite**

Run: `cd backend && uv run pytest -v`
Expected: PASS, all tests including the new `test_catalog_template_builder.py` and `test_catalog_template_artifact.py`.

- [ ] **Step 2: Confirm `git status` is clean and only expected paths changed**

Run: `git status`
Expected: only `backend/app/catalog_template/`, `backend/tests/test_catalog_template_*.py`,
`docs/02-domain/templates/`, `docs/02-domain/Catalog_Template.md`,
`docs/02-domain/Publication_Model.md`, `README.md` — nothing from `kwork/`.

- [ ] **Step 3: Follow the established delivery rhythm** (see project memory `greenmarket-workflow-rhythm`): build the colleague zip via `git archive` on tracked `README.md` + `docs/` + `database/migrations/` only, write `kwork/PR-008_GoogleSheetsTemplate_Review.md` (reality/why, note that negative-path structure/required-field testing is already covered generically by the existing PR-004/PR-005 test suites so wasn't duplicated here, open questions, "Файлы" list), push the branch, open a PR, wait for CI, and only merge after the colleague approves.

---

## Self-Review Notes

- **Spec coverage:** structure/columns (Task 2/3) · reference sheets & dropdowns (Task 2) · sheet protection (Task 2) · usability hints/examples/color-coding (Task 1/2) · Инструкция sheet (Task 1/2) · docs updates (Task 5) · testing empty/partial/full through the pipeline (Task 3/4) · out-of-scope items (Apps Script, auto-publish, webhooks, sync, macros, server-side generation) are correctly **not** implemented anywhere in this plan. Structural/required-field negative testing is intentionally not duplicated — already covered by existing `test_structure_validator.py`/`test_semantic_validator.py`.
- **Placeholder scan:** none found — every step has real code or a real command with expected output.
- **Type consistency:** `build_workbook(catalog_rows: list[list[object]] | None = None) -> Workbook` is used identically in `build.py`, `build_examples.py`, and both test files.
