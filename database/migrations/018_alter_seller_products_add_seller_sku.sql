-- Migration : 018_alter_seller_products_add_seller_sku.sql
-- Purpose   : Дефект 11.08.2026: каждая публикация каталога пересоздавала все
--             товары продавца заново (created 312, updated 0, deactivated 312).
--             Сопоставление строки книги с товаром шло только по SellerProductId,
--             а он присваивается сервером и в книгу попасть не может: Service
--             Account читает книгу и не пишет (CR-001), карточка товара
--             идентификатор не придумывает, эндпоинта со списком товаров нет,
--             публикация запускается из Личного кабинета — книга её ответа не
--             видит. seller_sku закрывает этот разрыв ключом, который
--             принадлежит продавцу: он есть в книге изначально и переживает
--             и перепубликацию, и пересоздание книги с нуля.
-- Note      : SellerProductId при этом не трогается и своей роли не меняет —
--             он равен SellerProduct.id, суррогатному первичному ключу, на
--             который ссылается SellerProductPhoto. Внутренний ключ и внешний
--             бизнес-ключ разделены намеренно.
-- Note      : NULL допустим. У книг шаблонов 2.1/2.2 колонки «Артикул продавца»
--             физически нет, и такие книги обязаны публиковаться по-прежнему —
--             пустой артикул это штатное состояние, а не ошибка.
-- Note      : UNIQUE (seller_id, seller_sku), а не UNIQUE (seller_sku):
--             артикул уникален внутри каталога одного продавца, у разных
--             продавцов PROD-1001 может означать разные товары. MySQL не
--             считает NULL равными между собой, поэтому строки без артикула
--             индексу не мешают и могут сосуществовать в любом количестве.
-- Note      : VARCHAR(64) — артикул это код учётной системы продавца
--             (PROD-1001), а не текст. sql_mode на проде пустой, длинные
--             строки обрезались бы молча, поэтому длину проверяет валидатор
--             публикации, а не только база.
-- DBMS      : MySQL Community Server 8.0.16+

ALTER TABLE SellerProduct
    ADD COLUMN seller_sku VARCHAR(64) NULL
        COMMENT 'Артикул товара в учётной системе продавца; ключ сопоставления строки книги с товаром. NULL — книга без колонки «Артикул продавца»'
        AFTER supply_date,
    ADD CONSTRAINT uk_SellerProduct_seller_sku UNIQUE (seller_id, seller_sku);
