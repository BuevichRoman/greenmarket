-- Migration : 019_alter_products_unique_name.sql
-- Purpose   : Наименование позиции единого справочника делается уникальным.
--             Catalog_Model.md (раздел Product) требует, чтобы позиция
--             создавалась один раз и использовалась всеми продавцами, но
--             ограничения в схеме под это не было: 18.08.2026 на проде
--             существовало 19 пар записей с одинаковым `name` и разными `id`.
--             Покупателю такая пара выглядит как две одинаковые плитки
--             каталога (группа на плитке не показывается), модератору — как
--             две неотличимые строки в поиске по справочнику, и к какой из них
--             привязано предложение, определял случай.
-- Note      : UNIQUE (name), а не UNIQUE (product_group_id, name). Уникальность
--             внутри группы оставила бы ровно те дубли, которые видит
--             покупатель: все существующие пары — это одно имя в двух разных
--             группах («Яблоки» во Фруктах и в Сухофруктах). Различать их
--             должно наименование, а не группа: покупатель группу не видит.
-- Note      : collation таблицы `utf8mb4_0900_ai_ci` регистронезависимая,
--             поэтому индекс не пропустит и «ЧЕРЕМША» рядом с «Черемша». Это
--             же правило сравнения использует проверка в Admin API — они не
--             должны расходиться (DDL_Specification.md: UNIQUE не заменяет
--             проверку бизнес-логики приложения, а дополняет её).
-- Note      : `idx_Product_name` снимается как избыточный — уникальный индекс
--             обслуживает те же обращения по имени (DDL_Specification.md:
--             индексы «на будущее» не создаются).
-- Warning   : миграция не применится, пока в таблице есть тёзки. Это
--             намеренно: молча выбрать «правильную» из двух позиций нельзя,
--             к обеим уже привязаны предложения продавцов. Перед накатом:
--
--               SELECT p.name, COUNT(*) AS c, GROUP_CONCAT(p.id) AS ids
--               FROM Product p GROUP BY p.name HAVING c > 1;
--
--             Пустой результат — предусловие выполнено.
-- DBMS      : MySQL Community Server 8.0.16+

ALTER TABLE Product
    DROP INDEX idx_Product_name,
    ADD CONSTRAINT uk_Product_name UNIQUE (name);
