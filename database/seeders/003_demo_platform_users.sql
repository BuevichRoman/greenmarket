/*
|--------------------------------------------------------------------------
| GreenMarket
|--------------------------------------------------------------------------
| Seeder    : 003_demo_platform_users.sql
| Purpose   : Демо-пользователи платформы для демо-продавцов (dev/CI/demo)
| DBMS      : MySQL Community Server 8.0.16+
|--------------------------------------------------------------------------
| ВНИМАНИЕ: этот файл вставляет строки в таблицу `users` — на реальном
| окружении это платформенная таблица aristotel_taxi.users, которой
| GreenMarket не владеет (см. backend/tests/fixtures/platform_stub.sql).
| Запускать ТОЛЬКО там, где `users` — тестовый стаб (локальный dev,
| CI) или где заведомо разрешено вставлять тестовых пользователей
| (например, отдельный demo-стенд с собственной копией платформенной БД).
|
| НЕ запускать на реальном production-сервере (aristotel_taxi) — там
| `users` содержит реальные учётные записи людей. Для демо-продавцов на
| проде нужны реальные `id_user` существующих аккаунтов с ролью Водитель,
| выделенные платформой специально для демонстрации — см.
| 004_demo_sellers_and_offers.sql, который от конкретного способа
| получения user_id не зависит.
|
| Запускать после стаба платформенных таблиц (platform_stub.sql), перед
| 004_demo_sellers_and_offers.sql.
|--------------------------------------------------------------------------
*/

SET NAMES utf8mb4;

INSERT INTO users (name) VALUES
    ('Демо: Ферма Ромашково'),
    ('Демо: Хутор Заречный'),
    ('Демо: Сад Плодовый');
